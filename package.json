{
  "name": "secur-node",
  "version": "1.0.10",
  "description": "Alliance Secur client lib for nodejs applications",
  "main": "lib/index.js",
  "scripts": {
    "package-publish": "npm run build && npm version patch --no-git-tag-version && npm publish",
    "build": "npm run format && tsc",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "alliance-sdk": "^1.0.4",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^8.5.1",
    "@types/node": "^15.3.0",
    "@types/nodemailer": "^6.4.2",
    "prettier": "^2.3.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "files": [
    "lib/**/*"
  ]
}
